apiVersion: v1
kind: Service
metadata:
  name: {{ template "debian.fullname" . }}
  labels:
    app: {{ template "debian.name" . }}
    chart: {{ template "debian.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: 31081
      protocol: TCP
      name: http
  selector:
    app: {{ template "debian.name" . }}
    release: {{ .Release.Name }}
