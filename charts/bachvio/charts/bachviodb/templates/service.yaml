apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: {{ template "bachviodb.name" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
      name: http
  selector:
    app: {{ template "bachviodb.name" . }}
